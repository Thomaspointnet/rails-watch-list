<h1>See all the lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= list.name %> - <%= link_to "See list details", list_path(list) %></li>
  <% end %>
</ul>
<h4> <%= link_to "Create a new list", new_list_path %> </h4>
