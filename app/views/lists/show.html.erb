<h1>List details</h1>
<h2> <%= @list.name %> </h2>
<h3>Movies</h3>
<ul>
  <% @bookmarks.each do |bookmark| %>
    <li> <img src="<%= bookmark.movie.poster_url %>" alt="<%= bookmark.movie.title %>" width= 200> <%= bookmark.movie.title %> - <%= bookmark.comment %></li>
  <% end %>
</ul>
<h4> <%= link_to "Add a bookmark", new_list_bookmark_path(@list) %> </h4>
<%# link_to "Remove", bookmark_path(@bookmark), method: :delete, data: { confirm: "Are you sure?" } %>
<h4> <%= link_to "Home", lists_path %> </h4>
